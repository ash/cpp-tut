#include <iostream>
#include <string>

using namespace std;

// ternary operator

int main() {
    // <condition> ? <value1> : <value2>;
    string result = 'a' < 'b' ? "OK" : "Not OK";
    cout << result << endl;
}
